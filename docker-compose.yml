version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8081:8081"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://192.168.100.215:5432/conteo_electoral
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=086411421
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  postgres_data:
